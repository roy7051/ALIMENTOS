<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alimentos">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ALIMENTOS">
        <activity android:name=".chatmain"></activity>
        <activity android:name=".food_item" />
        <activity android:name=".category" />
        <activity android:name=".GalleryActivity" />
        <activity android:name=".ngo_register" />
        <activity android:name=".signup_second_layout" />
        <activity android:name=".ProfileActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsFragment"
            android:label="@string/map_str" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".ImagesActivity" />
        <activity android:name=".login_activity" />
        <activity android:name=".upload_activity" />
        <activity android:name=".recycle_activity" />
        <activity android:name=".activity_med_list" />
        <activity
            android:name=".activity_nav"
            android:label="@string/title_activity_nav"
            android:theme="@style/Theme.ALIMENTOS.NoActionBar" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>